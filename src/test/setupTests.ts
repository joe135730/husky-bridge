// test initialization, test environment setup.
// It runs before each test file.
// Here we import @testing-library/jest-dom, which:
// Adds extra matchers to expect, like:
// toBeInTheDocument()
// toHaveTextContent()
// toBeVisible()
import '@testing-library/jest-dom'
